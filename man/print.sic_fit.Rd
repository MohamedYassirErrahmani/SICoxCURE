% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sic_fit.R
\name{print.sic_fit}
\alias{print.sic_fit}
\title{Print a Fitted SIC Model}
\usage{
\method{print}{sic_fit}(x, digits = 4L, ...)
}
\arguments{
\item{x}{An object of class \code{"sic_fit"}.}

\item{digits}{Integer. Number of significant digits to print.
Default: \code{4}.}

\item{...}{Additional arguments passed to \code{print}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Prints a compact summary of a fitted \code{"sic_fit"} object,
showing the main model parameters and convergence status.
}
\examples{
\dontrun{
set.seed(42)
n     <- 150
X     <- matrix(rnorm(n * 4), n, 4)
Z     <- matrix(rbinom(n, 1L, 0.6), n, 1)
p     <- 1 / (1 + exp(-X \%*\% c(1, -0.5, 0.3, -0.2)))
B     <- rbinom(n, 1L, p)
T0    <- ifelse(B == 1, rexp(n, 0.5), Inf)
C     <- rexp(n, 0.3)
Y     <- pmin(T0, C)
delta <- as.integer(T0 <= C & T0 < Inf)

fit <- sic_fit(Y, delta, X, Z, h_step = 0.1, verbose = 0)
print(fit)
summary(fit)
}

}
